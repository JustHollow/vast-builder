
// const { runFixture } = require('../helpers');
const { assert } = require('chai');
const createVast = require('../lib');

const testOptions = {
  throwOnError: true,
  logWarn: false
}

describe('VAST Validator', () => {
  it('should throw on not correct only tag', () => {

  });
  it('should throw on not correct only tag', () => {

  });
});